<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.FudAI.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#0C0C0C</item>
        <!-- Leave the icon unset so Android uses the active launcher alias icon. -->
        <item name="postSplashScreenTheme">@style/Theme.FudAI</item>
    </style>
</resources>
